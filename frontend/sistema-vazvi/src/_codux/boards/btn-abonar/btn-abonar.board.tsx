import { createBoard } from '@wixc3/react-board';
import { BtnAbonar } from '../../../components/btn-abonar/btn-abonar';

export default createBoard({
    name: 'BtnAbonar',
    Board: () => <BtnAbonar />
});
