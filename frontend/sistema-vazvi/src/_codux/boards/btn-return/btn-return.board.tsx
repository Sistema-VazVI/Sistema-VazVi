import { createBoard } from '@wixc3/react-board';
import { BtnReturn } from '../../../components/btn-return/btn-return';

export default createBoard({
    name: 'BtnReturn',
    Board: () => <BtnReturn />
});
